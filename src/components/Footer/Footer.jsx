import s from "./Footer.module.css";

const Footer = (props) => {
    return (
        <div className = {s.row} >
            <div className = {s.title} >
                moviedb
            </div>
        </div>
    )
}

export default Footer;